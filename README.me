
1 JetBrain Idea编译时会出错，报：
  Error:java: 服务配置文件不正确, 或构造处理程序对象javax.annotation.processing.Processor: Provider me.generator.GenerateInterfaceProcessor not found时抛出异常错误

  处理: 需要将javax.annotation.processing.Processor文件中的Processor一行删掉，然后make两次。

2 使用该jar包sh时，编译出来的接口.java文件所在的目录，应直接设为source path。如果只是把接口复制到src目录下，下次编译时会报Duplicated Java Class.
